import React from 'react';
import { Text, View, StyleSheet } from 'react-native';

const InterestsScreen = props => {
    return (
        <View>
            <Text>This is the InterestsScreen!</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default InterestsScreen;