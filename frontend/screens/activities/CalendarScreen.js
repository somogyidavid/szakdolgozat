import React from 'react';
import { Text, View, StyleSheet } from 'react-native';

const CalendarScreen = props => {
    return (
        <View>
            <Text>This is the CalendarScreen!</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default CalendarScreen;