import React from 'react';
import { Text, View, StyleSheet } from 'react-native';

const SightsScreen = props => {
    return (
        <View>
            <Text>This is the SightsScreen!</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default SightsScreen;