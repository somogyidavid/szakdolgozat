import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const SummaryScreen = props => {
    return (
        <View>
            <Text>This is the SummaryScreen!</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default SummaryScreen;